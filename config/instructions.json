[
    {
        "name" : "nop",
        "description": "No operation",
        "params" : [],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name": "dint",
        "description":"Disable interrupts",
        "params" : [],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "mov",
        "description": "Move data from source to destination", 
        "params" : [{ "operands" : ["reg"] }, { "operands" : ["reg", "imm"] }],
        "examples": ["r1, r2", "r1, #5"],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "add",
        "description": "Sum two operands",
        "params" : [{ "operands" : ["reg"] }, { "operands" : ["reg", "imm"] }],
        "examples" : ["r1, r2", "r1, #5"],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "cmp",
        "description": "Compare two operands",
        "params" : [{ "operands" : ["reg", "?"] }, { "operands" : ["reg", "imm"] }],
        "examples" : ["r1, r2", "r1, #5"],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "jmp",
        "description": "Jump to label",
        "params" : [{ "operands" : ["label"] }],
        "examples" : ["label1"],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "push",
        "description": "Push to stack",
        "params" : [{ "operands" : ["reg", "imm"] }],
        "examples" : ["r1, #5"],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "rst",
        "description" : "Reset",
        "params" : [],
        "atom" : true,
        "available_for": ["enclave", "attacker"]
    },
    {
        "name" : "rst_nz",
        "description" : "Reset if not zero",
        "params" : [],
        "atom" : true,
        "available_for": ["attacker"]
    },
    {
        "name" : "ubr",
        "description" : "Unconditional branch",
        "params" : [{ "operands" : ["label"] }],
        "examples" : ["label1"],
        "atom" : true,
        "available_for": ["attacker"]
    }
]